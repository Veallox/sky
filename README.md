# Sky

Sky is a concept landing page for a browser-based ethical hacking platform that delivers Linux workspaces, terminals, and vetted security tools directly in the browser.

## Preview

Open `index.html` in your browser or run a quick local server:

```bash
python3 -m http.server 8080
```

Then visit `http://localhost:8080`.
